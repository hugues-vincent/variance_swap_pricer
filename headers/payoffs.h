#include <iostream>
#include <cmath>
#include <fstream>
#include <string>

#include <numeric>
#include <vector>
#include <iterator>
#include <functional>

#include "../utils/utils.h"
#include "./ProcessGenerator.h"


typedef double (*Payoff)(ProcessGenerator &process);

double var_swap_payoff(ProcessGenerator &process)
{
	
	return 0;
}